#include <stdio.h>
/**
 * Discription: main - prints the alphabet in lower case
 *
 * Return: 0 if successful
 *
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z' ; a++)
putchar(a);
putchar('\n');
return (0);
}
